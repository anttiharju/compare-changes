# semver-label

If mandated via `require:` input, fails if the pr associated with the commit does not have an appropriate label (`patch`, `minor`, or `major`). Outputs the next semantic version for use in downstream actions.

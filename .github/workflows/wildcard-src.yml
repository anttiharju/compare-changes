permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - "src/**/*"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true

permissions: {}
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/action/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard/release-action.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true

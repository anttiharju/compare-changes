permissions: {}
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/action-compare/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard/release-action-compare.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true

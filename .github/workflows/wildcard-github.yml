permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".build/archive.sh"
      - ".release/actions_env_mock.sh"
      - "flake.nix"
      - "flake.lock"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true

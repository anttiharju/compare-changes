permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/nix/**/*"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true

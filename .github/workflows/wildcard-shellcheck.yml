permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - "**/*.sh"
      - ".shellcheckrc"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
